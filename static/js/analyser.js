 var canvasElement = document.getElementById("barChart")
    var = config = {
        type: "bar",
        data: {
        labels: [],
        datasets: [],
        },
    }
    var barChart = new Chart(canvasElement,config)